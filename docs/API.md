# API Documentation Template

Use this file as the canonical API contract reference.

## Conventions
- Base URL: `/api` (or environment-specific gateway path)
- Response format: JSON
- Error format: include machine-readable `code` and human-readable `message`

## Endpoint Template
### `METHOD /resource`

**Purpose**
Describe what the endpoint does.

**Request**
- Query params:
- Path params:
- Body schema:

**Response**
- Status codes:
- Success schema:
- Error schema:

**Notes**
- Authorization requirements
- Side effects
- Idempotency expectations
